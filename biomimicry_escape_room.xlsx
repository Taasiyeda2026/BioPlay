:root {
  --bg-1: #0b1025;
  --bg-2: #1a1a3a;
  --card: rgba(255, 255, 255, 0.1);
  --text: #f8f5ff;
  --accent: #81f2d1;
  --accent-2: #ffd77a;
}

* {
  box-sizing: border-box;
  font-family: 'Rubik', 'Segoe UI', sans-serif;
}

body {
  margin: 0;
  min-height: 100vh;
  background: radial-gradient(circle at top, #22307a, var(--bg-1) 48%, #070b18);
  color: var(--text);
}

.screen {
  min-height: 100vh;
  display: grid;
  place-items: center;
  padding: 1.2rem;
}

.card {
  width: min(920px, 100%);
  background: var(--card);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 24px;
  padding: 1.2rem 1.1rem;
  backdrop-filter: blur(8px);
  box-shadow: 0 12px 40px rgba(0, 0, 0, 0.3);
}

.slide-card {
  width: min(1080px, 100%);
}

.slide-layout {
  display: grid;
  gap: 1rem;
}

.slide-content h2 {
  margin-bottom: 0.7rem;
}

.slide-image-panel {
  background: rgba(255, 255, 255, 0.08);
  border: 1px solid rgba(255, 255, 255, 0.24);
  border-radius: 18px;
  padding: 0.7rem;
}

.slide-image {
  width: 100%;
  display: block;
  border-radius: 14px;
  aspect-ratio: 4 / 3;
  object-fit: cover;
  background: rgba(7, 11, 24, 0.4);
}

.image-caption {
  margin: 0.55rem 0 0;
  font-size: 0.92rem;
  text-align: center;
  color: var(--accent-2);
}

.glow {
  box-shadow: 0 0 50px rgba(129, 242, 209, 0.25);
}

h1,
h2,
h3,
p {
  margin-top: 0;
}

.eyebrow {
  color: var(--accent-2);
  font-weight: 700;
}

.story {
  line-height: 1.8;
  max-height: 40vh;
  overflow: auto;
  padding-inline-end: 0.5rem;
}

.primary-btn,
.secondary-btn,
.ghost-btn,
.option-btn,
.match-card,
.word-btn,
.door-btn {
  border: none;
  border-radius: 14px;
  padding: 0.75rem 1rem;
  font-size: 1rem;
  cursor: pointer;
}

.primary-btn {
  background: linear-gradient(90deg, var(--accent), #60d4ff);
  color: #072340;
  font-weight: 700;
}

.secondary-btn {
  background: rgba(255, 255, 255, 0.15);
  color: white;
}

.ghost-btn {
  background: transparent;
  border: 1px solid rgba(255, 255, 255, 0.35);
  color: white;
}

.door-grid,
.options,
.match-grid,
.cipher-words {
  display: grid;
  gap: 0.8rem;
}

.door-grid {
  grid-template-columns: repeat(auto-fit, minmax(130px, 1fr));
}

.door-btn {
  min-height: 120px;
  background: linear-gradient(180deg, #532f8a, #2a1b4c);
  color: white;
  display: grid;
  place-items: center;
  border: 1px solid rgba(255, 255, 255, 0.25);
}

.icon {
  font-size: 2rem;
}

.options {
  margin: 1rem 0;
}

.option-btn,
.match-card,
.word-btn {
  background: rgba(255, 255, 255, 0.12);
  color: white;
  text-align: right;
}

.actions-row {
  display: flex;
  flex-wrap: wrap;
  gap: 0.6rem;
  margin-top: 0.9rem;
}

.hint {
  margin-top: 0.9rem;
  background: rgba(255, 215, 122, 0.2);
  border-radius: 10px;
  padding: 0.6rem;
}

.feedback {
  margin-top: 0.7rem;
  color: var(--accent);
  font-weight: 600;
}

.image-puzzle {
  display: grid;
  grid-template-columns: repeat(6, 1fr);
  gap: 0.35rem;
  background: rgba(255, 255, 255, 0.06);
  border-radius: 14px;
  padding: 0.7rem;
  margin-bottom: 0.7rem;
}

.spot {
  width: 100%;
  aspect-ratio: 1;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.15);
}

.spot.target {
  background: rgba(129, 242, 209, 0.9);
}

.answer-label,
.subtle {
  opacity: 0.9;
}

#image-answer {
  width: 100%;
  margin-top: 0.4rem;
  border-radius: 12px;
  border: none;
  padding: 0.8rem;
  font-size: 1rem;
}

.modal {
  position: fixed;
  inset: 0;
  background: rgba(2, 5, 15, 0.7);
  display: grid;
  place-items: center;
  padding: 1rem;
}

.modal-card {
  width: min(560px, 100%);
  background: #1b2248;
  border-radius: 16px;
  padding: 1rem;
  border: 1px solid rgba(255, 255, 255, 0.28);
}

.cipher-target {
  min-height: 58px;
  border: 1px dashed rgba(255, 255, 255, 0.4);
  border-radius: 14px;
  padding: 0.65rem;
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin-bottom: 0.7rem;
}

.cipher-target span {
  background: rgba(129, 242, 209, 0.2);
  padding: 0.35rem 0.7rem;
  border-radius: 999px;
}

.lock-open {
  margin-top: 0.8rem;
  color: var(--accent-2);
  animation: pulse 1.2s ease-in-out infinite;
}

.celebration {
  font-size: 2rem;
  text-align: center;
  margin: 1rem 0;
}

@keyframes pulse {
  0%,
  100% {
    opacity: 0.7;
  }
  50% {
    opacity: 1;
  }
}

@media (min-width: 760px) {
  .card {
    padding: 1.8rem;
  }

  .slide-layout {
    grid-template-columns: 1.2fr minmax(250px, 0.8fr);
    align-items: start;
  }

  .options {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

.answer-index {
  color: var(--accent-2);
  font-weight: 700;
}

.habitat-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(130px, 1fr));
  gap: 0.7rem;
  margin-top: 0.8rem;
}

.habitat-card {
  border: 1px solid rgba(255, 255, 255, 0.28);
  border-radius: 14px;
  background: rgba(255, 255, 255, 0.08);
  color: white;
  padding: 0.55rem;
  display: grid;
  gap: 0.45rem;
  text-align: center;
}

.habitat-card img {
  width: 100%;
  aspect-ratio: 1;
  object-fit: cover;
  border-radius: 12px;
  background: rgba(7, 11, 24, 0.5);
}

.code-input {
  width: 100%;
  border-radius: 12px;
  border: none;
  padding: 0.8rem;
  font-size: 1.05rem;
}

.transition-card {
  text-align: center;
  animation: pulse 1.1s ease-in-out infinite;
}
