{
  "name": "bio-magic",
  "version": "1.0.0",
  "private": true,
  "description": "Bio-mimicry escape room game",
  "scripts": {
    "build:data": "node scripts/build-game-data.js",
    "check:data": "node scripts/build-game-data.js && python scripts/validate_data.py"
  }
}
